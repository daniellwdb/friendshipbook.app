import { theme } from "@chakra-ui/react";

const typography = {
  fonts: {
    ...theme.fonts,
    heading: "Chelsea Market, DM Sans, Helvetica, Arial, sans-serif",
    body: "DM Sans, Helvetica, Arial, sans-serif",
  },
};

export default typography;
